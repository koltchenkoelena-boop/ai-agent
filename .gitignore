# secrets / local env
.env
*.key

# shared runtime state + user data (PII, sessions db, mailbox, results)
shared/

# qwen-code per-project + worktrees
.qwen/

# runtime state
sandbox/
logs/
pids/
*.pid
__pycache__/
*.pyc
myenv/
venv/
.venv/

# editor/sync backups
*.bak
*.bak.*
