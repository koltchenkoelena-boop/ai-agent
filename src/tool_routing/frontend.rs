// ---------------------------------------------------------------------------
// Frontend tools — прокси на UI (WebSocket / gRPC)
//
// TO DO: Реализовать транспорт для фронтенд-тулов.
//
// Концепция:
//   - Агент вызывает тул, который требует визуального отображения (например,
//     подтверждение от пользователя, выбор из списка, просмотр изображения).
//   - ToolRouter определяет, что тул помечен как Frontend.
//   - Вызов сериализуется и отправляется на фронтенд через WebSocket/gRPC.
//   - Фронтенд обрабатывает запрос и возвращает результат.
//   - Агент получает результат и продолжает работу.
//
// Пока заглушка — будет реализована после интеграции WebSocket/gRPC транспорта.
// ---------------------------------------------------------------------------

/// Placeholder — заглушка для фронтенд тулов.
pub struct FrontendTransport;

impl FrontendTransport {
    /// Инициализировать подключение к фронтенду.
    /// Пока паника — будет реализовано позже.
    pub async fn connect(_url: &str) -> Result<Self, String> {
        todo!("FrontendTransport::connect — реализовать после интеграции WebSocket/gRPC")
    }
}
