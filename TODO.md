# TODO

- Инструмент записи экрана (скриншот/видео) для Claude Code: обернуть системный
  рекордер (wf-recorder/ffmpeg x11grab) в CLI-скрипт, вызываемый через Bash —
  чтобы можно было запускать/останавливать запись по запросу. Не встроено,
  нужно делать отдельно, каждый запуск потребует явного разрешения пользователя.
